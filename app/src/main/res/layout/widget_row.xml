<?xml version="1.0" encoding="utf-8"?>
<!--
  One story in the widget.

  Both text views ellipsize at their end: android:ellipsize with singleLine is
  the only eliding RemoteViews honours (marquee needs a focused view, which a
  widget row never is), and the byline is the line most likely to overflow.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_outer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/row_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingTop="7dp"
        android:paddingBottom="7dp">

        <ImageView
            android:id="@+id/row_image"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginEnd="9dp"
            android:contentDescription="@string/article_image"
            android:scaleType="centerCrop" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/row_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/widget_title"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/row_byline"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:paddingTop="1dp"
                android:singleLine="true"
                android:textColor="@color/widget_meta"
                android:textSize="11sp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/row_meta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:paddingTop="1dp"
                android:singleLine="true"
                android:textColor="@color/widget_meta"
                android:textSize="11sp" />
        </LinearLayout>
</LinearLayout>

    <!--
      The row's own hairline, because a ListView's android:divider is a
      resource the launcher resolves and so cannot follow this widget's theme.
      WidgetService colours it per row.

      An ImageView, not a View. RemoteViews inflates only the classes on its
      whitelist (FrameLayout, LinearLayout, RelativeLayout, GridLayout, and the
      handful of widgets: TextView, ImageView, ImageButton, Button, ProgressBar,
      ListView, …) — and a bare <View> is not one of them. Using one made every
      row fail to inflate, which the launcher reports, once per row, as
      "Couldn't add widget."
    -->
    <ImageView
        android:id="@+id/row_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:contentDescription="@null"
        android:importantForAccessibility="no" />
</LinearLayout>
